<p id="notice"><%= notice %></p>

<h3>
  <%= @plot.user.name %>'s
  <% plant = Plant.find(@plot.plant_id) %>
  <%= plant.name.pluralize %>
</h3>

<p>
  <strong>Community:</strong>
  <%= Community.find(@plot.community_id).name %>
</p>

<%= image_tag plant.image, class: 'large-plant-image' %> <br>

<%= link_to 'Destroy', plot_path(@plot), method: :delete %> 
<%= link_to 'Back', plots_path %>
